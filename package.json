{
  "name": "scout-backend",
  "version": "1.0.0",
  "description": "Backend for the Scout AI sourcing/pricing agent demo. Keeps the Groq API key server-side.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
